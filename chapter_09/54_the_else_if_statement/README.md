## Chapter 9: Understanding Basic Static Analysis

### The else-if statement
```c
void func(int x)
{
    if (x > 0x100) {
        puts("x > 0x100");
    } else if (x < 0x300) {
        puts("x > 0x100 and x < 0x300");
    } else {
        puts("x is else");
    }
}
```
- 위 코드에는 의도한 건지는 모르겠지만, 버그로 보이는 부분이 있다.
  - **"x is else"** 는 절대 출력될 수 없다.
  - Radare2를 이용해 Function Graph를 그려 보면, **"x is else"** 를 출력하는 코드가 컴파일러에 의해 삭제된 걸 확인 할 수 있다. (Unreachable Code)

```
                                          ┌─────────────────────────────────────┐
                                          │  0x7e8                              │
                                          │ ; arg1                              │
                                          │ 32: sym.func (signed int64_t arg1); │
                                          │ `- args(x0)                         │
                                          │ cmp w0, 0x100; {                    │
                                          │ b.le 0x7fc                          │
                                          └─────────────────────────────────────┘
                                                  f t
                                                  │ │
                                                  │ └───────┐
    ┌─────────────────────────────────────────────┘         │
    │                                                       │                                                                           
┌──────────────────────────────────────────────────┐    ┌──────────────────────────────────────────────────────────────────────────────┐
│  0x7f0                                           │    │  0x7fc                                                                       │
│ adrp x0, 0; void func(int x)                     │    │ ;[x] CODE XREF from sym.func @ 0x7ec(x)                                      │
│ ; 0x824                                          │    │ adrp x0, 0; } else if (x < 0x300) {                                          │
│ ; "x > 0x100"                                    │    │ ; 0x82e                                                                      │
│ add x0, x0, str.x___0x100; puts("x > 0x100");    │    │ ; "x > 0x100 and x < 0x300"                                                  │
└──────────────────────────────────────────────────┘    │ add x0, x0, str.x___0x100_and_x___0x300; puts("x > 0x100 and x < 0x300");    │
    v                                                   │ b 0x7f8                                                                      │
    │                                                   └──────────────────────────────────────────────────────────────────────────────┘
    │                                                       v
    │                                                       │
    └─────────────────────────────────────────┐             │
                                              │ ┌───────────┘
                                              │ │                                  
                                        ┌─────────────────────────────────────────┐
                                        │  0x7f8                                  │
                                        │ ;[x] CODE XREF from sym.func @ 0x804(x) │
                                        │ b sym.imp.puts; puts("x > 0x100");      │
                                        └─────────────────────────────────────────┘
```
